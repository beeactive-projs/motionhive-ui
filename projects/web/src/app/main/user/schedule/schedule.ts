import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'mh-user-schedule',
  template: '<p>Schedule works</p>',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class Schedule {}
