import {
  addOutline,
  alertCircleOutline,
  chatbubbleOutline,
  checkmarkCircleOutline,
  chevronBack,
  chevronForward,
  closeOutline,
  cloudOfflineOutline,
  compassOutline,
  createOutline,
  exitOutline,
  hourglassOutline,
  imageOutline,
  linkOutline,
  lockClosedOutline,
  locationOutline,
  peopleOutline,
  personAddOutline,
  personOutline,
  searchOutline,
  sendOutline,
  settingsOutline,
  shareOutline,
  shieldCheckmarkOutline,
  thumbsUp,
  thumbsUpOutline,
  trashOutline,
} from 'ionicons/icons';

/**
 * Every icon the Groups hub renders. The page calls `addIcons(GROUP_ICONS)`
 * once, so a name in a template can never reference an icon nobody
 * registered — an unregistered name renders as a blank box with no error
 * anywhere, which is exactly the kind of thing that ships.
 *
 * Pushed screens add their own sets as they land; the drift spec that fails
 * the build when this and the templates disagree comes with them, since it
 * works by scanning templates that do not exist yet.
 */
export const GROUP_ICONS = {
  addOutline,
  alertCircleOutline,
  chatbubbleOutline,
  checkmarkCircleOutline,
  chevronBack,
  chevronForward,
  closeOutline,
  cloudOfflineOutline,
  compassOutline,
  createOutline,
  exitOutline,
  hourglassOutline,
  imageOutline,
  linkOutline,
  lockClosedOutline,
  locationOutline,
  peopleOutline,
  personAddOutline,
  personOutline,
  searchOutline,
  sendOutline,
  settingsOutline,
  shareOutline,
  shieldCheckmarkOutline,
  thumbsUp,
  thumbsUpOutline,
  trashOutline,
};
