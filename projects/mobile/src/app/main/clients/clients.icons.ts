import {
  alertCircleOutline,
  archiveOutline,
  arrowUndoOutline,
  barbellOutline,
  chatbubbleOutline,
  checkmarkCircle,
  checkmarkCircleOutline,
  chevronForward,
  closeCircleOutline,
  cloudOfflineOutline,
  createOutline,
  ellipsisVertical,
  hourglassOutline,
  lockClosedOutline,
  mailOutline,
  peopleOutline,
  personAddOutline,
  personOutline,
  searchOutline,
  shareOutline,
} from 'ionicons/icons';

/**
 * Every icon the clients screens render. Each page calls
 * `addIcons(CLIENT_ICONS)` once, so a name in a template can never reference
 * an icon nobody registered — and `clients.config.spec.ts` fails the build
 * if this drifts from the names the templates actually use, in either
 * direction.
 */
export const CLIENT_ICONS = {
  alertCircleOutline,
  archiveOutline,
  arrowUndoOutline,
  barbellOutline,
  chatbubbleOutline,
  checkmarkCircle,
  checkmarkCircleOutline,
  chevronForward,
  closeCircleOutline,
  cloudOfflineOutline,
  createOutline,
  ellipsisVertical,
  hourglassOutline,
  lockClosedOutline,
  mailOutline,
  peopleOutline,
  personAddOutline,
  personOutline,
  searchOutline,
  shareOutline,
};
