import {
  banOutline,
  chatbubblesOutline,
  checkmarkCircle,
  checkmarkDone,
  close,
  createOutline,
  informationCircleOutline,
  personOutline,
  searchOutline,
  send,
} from 'ionicons/icons';

/**
 * Every icon the messages screens render. Each page calls
 * `addIcons(MESSAGING_ICONS)` once, so a name in a template can never
 * reference an icon nobody registered.
 */
export const MESSAGING_ICONS = {
  banOutline,
  chatbubblesOutline,
  checkmarkCircle,
  checkmarkDone,
  close,
  createOutline,
  informationCircleOutline,
  personOutline,
  searchOutline,
  send,
};
